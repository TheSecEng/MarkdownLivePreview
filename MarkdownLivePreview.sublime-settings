{
	// minimum number of milliseconds to wait before updating the preview again
	"delay_between_updates": 100,
	// Array for syntax's that this should be enabled on
	"syntax": ["Markdown", "MultiMarkdown"],
}